#!/bin/sh
DIR="$(cd "$(dirname "$0")" && pwd)"
NAME="password_gen"

if [ -d "/data/data/com.termux" ]; then
    COMPILER_FLAGS=""
else
    COMPILER_FLAGS="-static -nostdlib -fno-builtin"
fi

. "$DIR/../tools/compile_wrapper"
